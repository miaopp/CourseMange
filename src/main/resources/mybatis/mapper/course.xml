<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Course">
    <resultMap id="courseResultMap" type="Course">
        <result property="id" column="course_id"/>
        <result property="name" column="course_name"/>
        <result property="targetClass" column="target_class"/>
        <result property="courseBeginWeek" column="begin_week"/>
        <result property="courseEndWeek" column="end_week"/>
    </resultMap>
    <select id="selectCourses" resultMap="courseResultMap">
        <![CDATA[
            select *
            from course;
        ]]>
    </select>
    <insert id="insertCourse" parameterType="Course">
        <![CDATA[
            insert into course
            (course_name, target_class, begin_week, end_week)
            values (#{name}, #{targetClass}, #{courseBeginWeek}, #{courseEndWeek});
        ]]>
    </insert>
    <delete id="deleteCourse" parameterType="java.lang.Integer">
        <![CDATA[
            delete
            from course
            where course_id = #{id};
        ]]>
    </delete>
</mapper>